import { DEMO_NOW, TIMEZONE } from '@/lib/contracts';
export function AppHeader({date, active}: {date: string; active: 'schedule' | 'history'}) { return <header className="site-header"><div className="header-inner"><a className="brand" href="/" aria-label="Bright Path home"><span className="brand-mark"><span /></span><span>bright path<span className="brand-caption">LEARNING CENTRE</span></span></a><nav className="page-nav" aria-label="Main navigation"><a href={`/?date=${date}`} aria-current={active === 'schedule' ? 'page' : undefined}>Schedule</a><a href={`/history?date=${date}`} aria-current={active === 'history' ? 'page' : undefined}>History</a></nav><details className="demo-info"><summary>Demo</summary><div><p>Sample data</p><p>Today: {new Date(DEMO_NOW).toLocaleString('en-GB', { timeZone: TIMEZONE, day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit', hour12: false })} · UTC+7</p></div></details></div></header>; }
